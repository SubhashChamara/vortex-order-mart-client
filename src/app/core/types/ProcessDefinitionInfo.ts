export interface ProcessDefinitionInfo {
  name: string;
  processDefinition: string;
  groupId: string;
  groupName: string;
  taskCount: number;
  serviceUrl: string;
  logo: string;
  processId: string;
}
